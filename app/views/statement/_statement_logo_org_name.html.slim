- if request.format.to_s.include?('pdf')
  = wicked_pdf_image_tag "/images/logos/#{location.organization.logo}"
- else
  = image_tag "/images/logos/#{location.organization.logo}"
.title-break
h3 #{location.name}
